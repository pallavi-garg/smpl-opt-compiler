main
var a, b, c, d, e, f;
{
	let a <- call InputNum();
	let b <- a + 1;
	if a>0 then
		
		let c <- a + 2;

		if a>0 then
			let d <- a + 3;
		else
			let f <- a + 7;
		fi;

		let e <- a + 4;
	fi;
	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
	call OutputNum(d);
	call OutputNum(e);
	call OutputNum(f);
}.
