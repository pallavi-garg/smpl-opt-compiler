main
var a, b, c, d, e;
{
	let a <- call InputNum();
	let b <- a;
	let c <- b;
	let d <- b + c;
	let e <- a + b;
	if a<0 then
		let d<-d+e;
		let a<-d
	else 
		let d<-e
	fi;
	let a <-99;
	if a<0 then
		let d<-d+e;
		let a<-d;
		if a > 35 then
			let a <- 35*a
		fi;
	else 
		let d<-e
	fi;
	call OutputNum(a)
}.
