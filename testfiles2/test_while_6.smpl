main 
var a,b; 
array[5] arr;
{
    let a <- 1;
    if arr[a] < 10 then
        call OutputNum(a);
        let arr[a] <- 10*8;
    fi;
    while arr[a] < 10 do
        let b <- a + 1;
        if a < 10 then
            let b <- 1 + 1;
        fi;
        call OutputNum(b);
        
    od;
    call OutputNum(b);
}.

#making cmp twice