main
var i, x;
array[4] a, b, c;
{
    let i <- 1;
    call OutputNum(a[1]);
    call OutputNum(b[1]);
    if (i == 1)
    then
        while(a[1] + b[1] >=0) do
            let a[1] <- x - 1;
            call OutputNum(a[1]);
        od;

        while(a[1]>=0) do
            let a[1] <- x - 1;
            call OutputNum(a[1]);
        od;

    fi;

    call OutputNum(a[1]);
    call OutputNum(b[1]);
}.