main
var i, x;
array[4] a,b;
{
    let i <- 1;
    let a[i] <- 0;
    let x <- a[i]; 
    while ((a[i] + (9+10))) == b[10] do
        let a[i] <- x - 1;
    od;
    call OutputNum(i);
    call OutputNum(x);
    call OutputNum(a[i]);
}.